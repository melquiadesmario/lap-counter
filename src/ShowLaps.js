import React from 'react'

const ShowLaps = (props) => (
    <div className='container-laps'>
      <p className='text-laps'>{ props.laps }</p>
      <p>Laps</p>
    </div>
)

export default ShowLaps
